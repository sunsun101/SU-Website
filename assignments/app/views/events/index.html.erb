<div class="container">
  <div class="d-flex flex-row-reverse mt-3 mb-3">
    <%= form_with(url: events_path) do |form| %>
      <%= text_field_tag(:search, params[:search], class:'form-control', placeholder: 'Search Category')%>
    <% end %>
  </div>
</div>
<%= render 'list', events: @events%>

